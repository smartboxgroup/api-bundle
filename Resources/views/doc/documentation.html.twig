<p>{{ methodConfig.description }}</p>

<h4>Soap version</h4>
{% autoescape false %}
```php

$client->{{ methodName }}(array(
{% for inputName, inputConfig in methodConfig.input %}
    '{{ inputName }}' => ${{ inputName }},  //{{ inputConfig.description|default('') }} (Type: {{ inputConfig.type }}{% if inputConfig.group is defined %} Group: {{ inputConfig.group }}{% endif %})
{% endfor %}
));

```
{% endautoescape %}